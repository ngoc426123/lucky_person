@Plugin({
  options: {
    dataListPerson: '[data-list-person]',
    dataBtnSpin: '[data-btn-spin]',
  }
})
export default class Loading {
  init () {
    this.initDOM();
    this.handleEvent();
  }

  initDOM () {

  }

  handleEvent () {
    
  }
}